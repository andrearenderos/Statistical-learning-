Groupinf unabled examples
the task of dividing the population or data points into a number of groups such that data points in the same groups are more.
Based in simple ideas. 
LLoyds algorithm. For examples we can choose K random centers, aka centroids.
